
  function shareText(){return `I reached Level ${level} and landed ${stats.interviews} interview${stats.interviews===1?'':'s'} in The Job Hunt. I also made ${stats.moves} career move${stats.moves===1?'':'s'} outside the application funnel. Score: ${stats.score.toLocaleString()}. Your turn.`;}
  async function share(){const text=shareText();try{if(navigator.share){await navigator.share({title:'The Job Hunt',text,url:location.href});}else{await navigator.clipboard.writeText(text);showToast('Score copied — paste it into LinkedIn.');}}catch(e){if(e?.name!=='AbortError'){try{await navigator.clipboard.writeText(text);showToast('Score copied.');}catch(_){showToast(text);}}}}

  function loop(now){if(state!=='play')return;const dt=Math.min(.033,(now-last)/1000||0);last=now;update(dt,now);draw(now);if(state==='play')raf=requestAnimationFrame(loop);}

  canvas.addEventListener('pointerdown',e=>{
    if(state!=='play')return;const r=canvas.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;
    for(let i=pickups.length-1;i>=0;i--){const p=pickups[i];if(pointIn(x,y,{x:p.x,y:p.y,w:p.w,h:p.h})){collectPickup(p);return;}}
    if(y>H-78)return;
    fireCV(false);
  });
  playBtn.addEventListener('click',startGame);againBtn.addEventListener('click',startGame);shareBtn.addEventListener('click',share);phoneBtn.addEventListener('click',e=>{e.stopPropagation();useHuman();});
  window.addEventListener('resize',resize);resize();draw(performance.now());
})();
